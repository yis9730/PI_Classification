# Main environment for data preparation, training, evaluation, statistics,
# Sankey plots, and centroid representative-image selection.
#
# UMAP uses an intentionally separate environment: its recorded workflow needs
# NumPy 1.26.x while the recorded training/evaluation stack uses NumPy 2.2.6.
# See docs/ENVIRONMENTS.md.
-r requirements_train_eval.txt
